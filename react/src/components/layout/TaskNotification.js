import React from 'react';
import { notification } from 'antd';

import React, { Component } from 'react';

export default class TaskNotification extends Component {
  constructor(props) {
    super(props);
    this.state = {
      tasks: [],
    };
  }
  render() {
    return (
      <div>
        <React.Fragment></React.Fragment>
      </div>
    );
  }
}
